# Cloudflare Workers — static-assets-only deployment (no server code).
# Serves index.html / app.js / config.js directly from the repo root.
name = "bridge2-frontend"
compatibility_date = "2025-01-01"

[assets]
directory = "./"
